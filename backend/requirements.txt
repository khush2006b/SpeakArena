# ============================================================
# SpeakArena Backend — Python Dependencies
# Pin exact versions for reproducible builds.
# ============================================================

# --- Web Framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
gunicorn==23.0.0

# --- Database ---
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0          # Async PostgreSQL driver
psycopg2-binary==2.9.10  # Sync driver for Alembic migrations

# --- Redis ---
redis[hiredis]==5.2.1

# --- Authentication ---
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0        # Argon2id password hashing (OWASP recommended)
python-multipart==0.0.12   # For form data / file uploads

# --- Configuration ---
pydantic==2.10.3
pydantic-settings==2.6.1

# --- Storage (Cloudflare R2 via S3-compatible API) ---
boto3==1.35.81
botocore==1.35.81
certifi>=2024.2.2            # Up-to-date CA bundle for R2 TLS (Python 3.13 compat)

# --- Payments ---
razorpay==1.4.2

# --- Email ---
fastapi-mail==1.4.1

# --- Background Tasks / Scheduling ---
apscheduler==3.10.4

# --- Monitoring ---
sentry-sdk[fastapi]==2.19.2

# --- Utilities ---
python-slugify==8.0.4
pillow==11.0.0            # Image processing for avatar thumbnails
aiofiles==24.1.0          # Async file operations
httpx==0.27.2             # Async HTTP client (for external calls)
orjson==3.10.12           # Fast JSON serialization

# --- Security ---
python-magic==0.4.27      # MIME type detection via magic bytes
bandit==1.8.0             # Security linting (dev/CI)

# --- Development / Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-mock==3.14.0           # mocker fixture for clean unittest.mock usage
httpx==0.27.2             # Used in tests for async API calls
factory-boy==3.3.1        # Test fixtures
faker==33.1.0             # Fake data generation
ruff==0.8.4               # Linting + formatting
mypy==1.13.0              # Type checking
pip-audit==2.7.3          # Dependency vulnerability scanning
setuptools<70.0.0
